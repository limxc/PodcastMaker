node_modules
dist
.DS_Store
.env
out

# Local config — machine-specific, not shared
.claude/settings.local.json

# Runtime user prefs (generated from template at first run)
user_prefs.json

# Pipeline output
videos/
